[project]
name = "vortex-scripts"
version = "0.1.0"
description = "Development and operational scripts for the Vortex monorepo"
requires-python = ">=3.11"
dependencies = [
    "hf-xet>=1.1.10",
    "httpx>=0.27",
    "huggingface-hub>=0.34.0",
    "pyarrow>=17.0.0",
]

[tool.uv]
package = false
